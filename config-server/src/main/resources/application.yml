server:
  port: 8855

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/javayp/netflix-pack-stack/tree/master/device-global-config
          search-paths:
          - 'device*'
          #username:
          #password:
